angular.module('assessment').controller('productsCtrl', function($scope, $stateParams, productService) {
  if($stateParams.id === 'jvmquxr') {
    $scope.productData = productService.shoeData;
  }
  if($stateParams.id === '9ad1jor') {
    $scope.productData = productService.sockData;
  }
  if($stateParams.id === 'gqwu3di') {
    $scope.productData = productService.sockData;
  }
  if($stateParams.id === 'fav2t9') {
    $scope.productData = productService.sockData;
  }
  if($stateParams.id === 'rpy14i') {
    $scope.productData = productService.sockData;
  }
  if($stateParams.id === '4866flxr') {
    $scope.productData = productService.sockData;
  }
  if($stateParams.id === '27kqpvi') {
    $scope.productData = productService.sockData;
  }
});
